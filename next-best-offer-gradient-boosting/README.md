# Next Best Offer Gradient Boosting

Ranks marketing offers by predicted acceptance probability.

## Quick Start

```bash
pip install -r requirements.txt
python ml/training/train_gradient_boosting.py
uvicorn app.main:app --reload --port 8004
```

See `usermanual.md` for full guide.
